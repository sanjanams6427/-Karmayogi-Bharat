# ============================================================
# KB Translation System — Requirements
# RFB IN-KBL-543730-NC-RFB | iGOT Karmayogi
# ============================================================
# 1. Install PyTorch first (CUDA 12.1):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# 2. Then install everything else:
#    pip install -r requirements.txt
# ============================================================

# ── UI ───────────────────────────────────────────────────────
gradio>=4.44.0

# ── Deep Learning ────────────────────────────────────────────
transformers>=4.46.0
accelerate>=0.34.0
safetensors>=0.4.5
sentencepiece>=0.2.0
sacremoses>=0.1.1

# ── ASR ──────────────────────────────────────────────────────
faster-whisper>=1.1.0

# ── Translation ──────────────────────────────────────────────
# IndicTransToolkit: install from source (not on PyPI stable):
#   pip install git+https://github.com/AI4Bharat/IndicTransToolkit.git
IndicTransToolkit>=0.1.0

# ── TTS ──────────────────────────────────────────────────────
parler-tts>=1.0.0
# Coqui XTTS-v2 (voice cloning — install separately to avoid conflicts):
#   pip install coqui-tts --no-deps

# ── Audio ────────────────────────────────────────────────────
soundfile>=0.12.1
librosa>=0.10.2
scipy>=1.13.0
numpy>=1.26.0,<2.0
resampy>=0.4.3

# ── Video ────────────────────────────────────────────────────
imageio-ffmpeg>=0.5.1

# ── Language Detection ───────────────────────────────────────
lingua-language-detector>=2.0.2

# ── Document Processing ──────────────────────────────────────
python-docx>=1.1.2
openpyxl>=3.1.5
pdfplumber>=0.11.0

# ── CBP Portal Upload ────────────────────────────────────────
requests>=2.32.0

# ── HuggingFace Hub ──────────────────────────────────────────
huggingface_hub>=0.25.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.1

# ── Datasets (download + finetune scripts only) ──────────────
datasets>=3.0.0

# ── Fine-tuning (optional — finetune/ scripts only) ──────────
# deepspeed>=0.15.0       # ZeRO-3 multi-GPU (ds_zero3.json)
# bitsandbytes>=0.44.0    # 8-bit optimiser for single-GPU fine-tuning
